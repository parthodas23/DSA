const arr1 = [1, 2, 3, 4, 5, 6];
console.log(arr1.slice(0, 2)); // start from 0 to 2 index but 2 index wouldn't added
console.log(arr1.slice(2)); // from 2 to rest
console.log(arr1.slice(-2)); // last 2 value
