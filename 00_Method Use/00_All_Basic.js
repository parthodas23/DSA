const split = "hello".split("");
console.log(split); // push each string letter in an array

const sort = split.sort();
console.log(sort); // sort evey letter in an array

const join = sort.join("");
console.log(join); // convert the array in a string

const reverse = split.reverse();  
const join1 = reverse.join("");
console.log(join1);
